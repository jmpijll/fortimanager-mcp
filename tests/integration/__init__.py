"""Integration tests for FortiManager MCP server.

These tests require a real FortiManager instance and valid credentials.
All tests are designed to be non-intrusive (read-only when possible).
Test objects use the "MCP_TEST_" prefix for easy identification and cleanup.
"""

